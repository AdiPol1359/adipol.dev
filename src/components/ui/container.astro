---
import type { HTMLTag, Polymorphic } from 'astro/types';

type Props<T extends HTMLTag> = Polymorphic<{ as: T }>;

const {
	as: Tag = 'div',
	class: className,
	...props
} = Astro.props as Props<'div'>;
---

<Tag class:list={['mx-auto w-full max-w-320', className]} {...props}>
	<slot />
</Tag>
