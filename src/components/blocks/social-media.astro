---
import { cva } from 'cva';

import { socialMedia as socialMediaItems } from '@/data/social-media';

import type { VariantProps } from 'cva';

const socialMedia = cva({
	base: 'flex gap-x-4 *:text-7 *:transition-colors',
	variants: {
		color: {
			primary: '*:text-icon-secondary *:hover:text-icon-primary',
			secondary: '*:text-icon-tertiary *:hover:text-icon-secondary',
		},
	},
});

type Props = Required<VariantProps<typeof socialMedia>>;
---

<ul aria-label="Moje media społecznościowe" class={socialMedia(Astro.props)}>
	{
		socialMediaItems.map(({ icon: Icon, name, url }) => (
			<li>
				<a aria-label={name} href={url} target="_blank">
					<Icon />
				</a>
			</li>
		))
	}
</ul>
