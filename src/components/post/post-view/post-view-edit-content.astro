---
import { Button, Emoji, Heading, Paper } from '@/components/ui';

import type { Post } from '@/types/post';

interface Props {
	readonly post: Pick<Post, 'slug'>;
}

const CONTENT_URL = `https://github.com/AdiPol1359/adipol.dev/blob/main/posts/${Astro.props.post.slug}.md`;
---

<Paper
	as="article"
	variant="outline"
	class="flex flex-col items-center justify-center gap-y-4 py-10 laptop:py-14"
>
	<Heading as="h2" size="h3">
		<Emoji type="👉🏻" /> Zauważyłeś/aś błąd? <Emoji type="👈🏻" />
	</Heading>
	<div class="w-full max-w-76">
		<Button
			as="a"
			target="_blank"
			href={CONTENT_URL}
			color="brand"
			variant="outline"
		>
			Edytuj ten post na GitHubie!
		</Button>
	</div>
</Paper>
